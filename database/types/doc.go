// Package types holds shared persistence types embedded by domain models.
//
// [BaseModel] provides the common identifier and timestamp fields that models embed
// so entities across the codebase share a single, consistent base shape.
package types
