# Group 03 reuse audit — gokit

Scope: `chain/`, `dag/`, `worker/`, `pipeline/`, `stateful/`.

| Finding | File:line | L?-correct home module | Recommended action |
|---|---:|---|---|
| `chain.Executor` writes directly to package-global `log/slog` instead of receiving the kit `logger.Logger` through construction/options. | `chain/executor.go:6`, `chain/executor.go:111`, `chain/executor.go:121`, `chain/executor.go:173`, `chain/executor.go:181` | L1 `logger` consumed by L4 `chain` via injection | Align — add a functional option for an injected logger and remove global slog calls in the chain cleanup/execution path. |
| Worker supervisor implements its own exponential backoff calculation and cap. | `worker/supervisor.go:23`, `worker/supervisor.go:134` | L3 `resilience` backoff policy consumed by L4 `worker` | Align — replace `BackoffBase` arithmetic with an injected/configured `resilience.Backoff` strategy while keeping supervisor health state in `worker`. |
| Worker execution applies supervisor backoff using a local `time.NewTimer` sleep loop. | `worker/pool.go:301` | L3 `resilience` backoff/timeout primitives consumed by L4 `worker` | Align — have worker supervisor expose a resilience delay policy and centralize cancellation-aware waiting there. |
| Worker timeout middleware hand-rolls `context.WithTimeout` around every handler call. | `worker/middleware.go:25` | L3 `resilience.Timeout` consumed by L4 `worker` | Align — reimplement `WithTimeout` on top of the canonical resilience timeout policy so timeout semantics match the rest of the kit. |
| DAG cascade stages hand-roll per-stage `context.WithTimeout`. | `dag/cascade.go:316` | L3 `resilience.Timeout` consumed by L4 `dag` | Align — model stage timeouts as resilience timeout policies injected into cascade execution. |
| Stateful accumulator performs local `time.Since(lastFlush) < MinInterval` rate limiting. | `stateful/accumulator.go:156` | L3 `resilience` rate limiter consumed by L4 `stateful` | Align — express `MinInterval` as a resilience rate-limit policy or enhance resilience with a simple interval limiter if the current API is too request-shaped. |
| Pipeline batch/window/debounce/throttle timers are operator semantics, not retry/backoff/timeout policy. | `pipeline/batch.go:47`, `pipeline/window.go:64`, `pipeline/debounce.go:64`, `pipeline/throttle.go:1` | L4 `pipeline` | Leave — timers define stream windowing/throttling behavior and do not duplicate resilience control-flow primitives. |
| Stateful manager cleanup ticker owns TTL eviction cadence. | `stateful/manager.go:46` | L4 `stateful` | Leave — periodic TTL cleanup is module lifecycle behavior, not resilience retry/backoff/rate-limit logic. |
| Worker ticker/scheduler intervals are scheduled-job semantics. | `worker/ticker.go:157`, `worker/scheduler.go:1` | L4 `worker` | Leave — periodic worker execution belongs in worker scheduling and does not duplicate resilience. |
| Test-only `time.Sleep`/`time.After` calls appear across `dag`, `pipeline`, `worker`, and `stateful` tests. | `dag/*_test.go`, `pipeline/*_test.go`, `worker/*_test.go`, `stateful/*_test.go` | Test code | Leave — these are deterministic test synchronization/time-bound assertions; migrate opportunistically to channel/clock helpers when tests are touched. |
