// Package docker implements the [workload.Manager] interface backed by the Docker Engine SDK. Supports container lifecycle (deploy/start/stop/remove), log streaming, and label-based listing.
package docker
