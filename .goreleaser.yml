# Source-only library release: archive, complete source SBOM, checksum, and signature bundle.
version: 2

project_name: gokit

builds:
  - skip: true   # gokit ships libraries, not binaries.

source:
  enabled: true
  name_template: "{{ .ProjectName }}-{{ .Version }}-source"
  format: tar.gz

checksum:
  name_template: "checksums.txt"
  algorithm: sha256

sboms:
  - artifacts: source
    documents:
      - "{{ .ProjectName }}-{{ .Version }}.cdx.json"
    cmd: syft
    args: ["$artifact", "--output", "cyclonedx-json=$document", "--enrich", "all"]

signs:
  - cmd: cosign
    signature: "${artifact}.sigstore.json"
    args:
      - "sign-blob"
      - "--bundle=${signature}"
      - "${artifact}"
      - "--yes"
    artifacts: checksum
    output: true

release:
  github:
    owner: kbukum
    name: gokit
  # Toven owns the hosted GitHub Release and its commit-derived notes: `toven
  # release publish` cuts and pushes the tag and creates the Release before this
  # tag-triggered run. GoReleaser only uploads the supply-chain artifacts to that
  # existing Release — keep-existing prevents it from recreating the Release or
  # replacing Toven's notes body.
  mode: keep-existing
  prerelease: auto
  draft: false
  name_template: "{{ .Tag }}"
