// Package static implements the [discovery.Registry] interface backed by static configuration. Useful for tests, single-node deployments, and development environments where no real service registry is available.
package static
